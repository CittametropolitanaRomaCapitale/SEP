# Components folder

- contains all dumbs shared components related only to CMRC application
- all the common components MUST be moved to packages/ui if used by other applications inside the monorepo
