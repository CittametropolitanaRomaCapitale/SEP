fragment sid_process on SidProcessGraphqlDTO {
  bpmnProcessTaskId
  referenceName
  stato: statoAttivita
  determina {
    id
    cdr
    numProposta
    annoProposta
    ruDetermina
    annoDetermina
    oggetto
    stato
    tipo
    dataCreazioneString
  }
}
