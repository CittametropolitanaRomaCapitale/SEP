#import "../validazione_dto.fragment.graphql"

mutation updateValidaImpegnoAccertamento($determina_id: BigInteger) {
  valida_subImpegnoAccertamento(determina_id: $determina_id) {
    ...validazione_dto
  }
}
